# Shared helpers (pre-processing)

Use this folder for reusable helper functions used by multiple pre-processing notebooks (for example: path helpers, common plotting wrappers, or repeated table transforms).
